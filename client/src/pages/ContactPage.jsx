import { useState } from 'react';
import { api } from '../api';
import { useAuth } from '../contexts/AuthContext';
import PageHero from '../components/PageHero';
export default function ContactPage(){const{user}=useAuth();const[form,setForm]=useState({name:user?.fullName||'',email:user?.email||'',topic:'size-and-fit',message:''});const[feedback,setFeedback]=useState({type:'',text:''});const submit=async e=>{e.preventDefault();setFeedback({type:'',text:''});try{const d=await api('/contact',{method:'POST',body:form});setFeedback({type:'success',text:d.message});setForm(o=>({...o,message:''}));}catch(err){setFeedback({type:'error',text:err.message});}};return <><PageHero eyebrow="CUSTOMER SUPPORT" title="Contact StyleHaven" text="Messages are stored in MongoDB and can be progressed or resolved from the administrator dashboard."/><section className="section"><div className="container contact-grid"><div><p className="eyebrow">HELP BEFORE AND AFTER ORDERING</p><h2>Ask about size, fit, stock, returns or delivery</h2><p>Include the product name, preferred size and colour when asking for fit or availability guidance.</p><div className="contact-cards"><article><strong>Email</strong><span>support@stylehaven.demo</span></article><article><strong>Telephone</strong><span>+44 121 555 0142</span></article><article><strong>Hours</strong><span>Mon–Fri, 9:00–17:00</span></article></div></div><form className="form-card" onSubmit={submit}><label>Full name<input name="name" required minLength="2" maxLength="100" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email address<input name="email" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Topic<select name="topic" value={form.topic} onChange={e=>setForm({...form,topic:e.target.value})}><option value="size-and-fit">Size and fit</option><option value="order">Order</option><option value="delivery">Delivery</option><option value="returns">Returns or exchange</option><option value="stock">Stock</option><option value="product">Product information</option><option value="other">Other</option></select></label><label>Message<textarea name="message" rows="7" required minLength="10" maxLength="1500" value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/></label>{feedback.text&&<div className={`alert ${feedback.type}`}>{feedback.text}</div>}<button className="button primary" type="submit">Send message</button></form></div></section></>}
